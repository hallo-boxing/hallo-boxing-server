<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xiaoheshan.hallo.boxing.server.dal.dao.good.nfc.GoodNfcDOMapperExt">
  <!-- 扩展自动生成或自定义的SQl语句写在此文件中 -->
  <select id="selectGoodIdByNfcCode" parameterType="java.lang.Integer" resultType="java.lang.Integer">
    SELECT good_id
    FROM good_nfc
    WHERE nfc_code = #{nfcCode}
  </select>
</mapper>